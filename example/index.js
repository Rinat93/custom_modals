'use strict';
// import {Dialog_custom} from 'custom_dialogs';
const {Dialog_custom} = require('custom_dialogs/elements');
window.addEventListener('DOMContentLoaded', function() {
    new Dialog_custom('modal-dialog');
});